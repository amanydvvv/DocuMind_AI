# KueryCore AI Frontend Environment Template
# Copy this to .env and adjust as needed. VITE_* vars are baked into the
# production bundle at build time — never use http://localhost here for
# deployed builds.

# Backend API base URL (Render). No trailing slash needed.
VITE_API_URL=https://kuerycore-ai-97t5.onrender.com
